{
  "name": "vectorizer-saas-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test src/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
